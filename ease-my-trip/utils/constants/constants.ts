export enum TimeOut {
    DefaultMaxWaitTime = 180000,
    DefaultWaitTime = 30000,
    ElementWaitTime = 5000,
    LoadTimeOut = 60000,
    ShortWaitTime = 2000,
}

export enum KeyboardActions {
    ArrowDown = 'ArrowDown',
    ArrowUp = 'ArrowUp',
    Enter = 'Enter',
    Escape = 'Escape',
    Tab = 'Tab',
}